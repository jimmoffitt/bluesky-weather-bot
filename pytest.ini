[pytest]
testpaths = tests
markers =
    integration: hits live external APIs (Open-Meteo). No Bluesky credentials needed.
    live: sends real Bluesky posts/DMs. Requires BSKY_HANDLE + BSKY_APP_PASSWORD in .local.env.
